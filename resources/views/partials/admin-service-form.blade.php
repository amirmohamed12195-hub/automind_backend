@php $editing = $service !== null; @endphp
<form class="dialog-form" method="POST" action="{{ $editing ? route('admin.catalog.services.update', $service) : route('admin.catalog.services.store') }}">@csrf @if($editing) @method('PATCH') @endif<div class="form-grid"><label><span>Code</span><input name="code" value="{{ $service?->code }}" @readonly($editing) required></label><label><span>English name</span><input name="name_en" value="{{ $service?->name_en }}" required></label><label class="full"><span>Arabic name</span><input name="name_ar" dir="rtl" value="{{ $service?->name_ar }}" required></label><label><span>Default months</span><input name="default_month_interval" type="number" min="1" value="{{ $service?->default_month_interval }}"></label><label><span>Default kilometers</span><input name="default_km_interval" type="number" min="1" value="{{ $service?->default_km_interval }}"></label></div><input type="hidden" name="active" value="0"><label class="check-row"><input type="checkbox" name="active" value="1" @checked($service?->active ?? true)><span><strong>Active service</strong><small>Available for maintenance records and reminders.</small></span></label><div class="dialog-actions"><button type="button" class="admin-button secondary" data-close-dialog>Cancel</button><button class="admin-button primary" type="submit">{{ $editing ? 'Save service' : 'Create service' }}</button></div></form>
