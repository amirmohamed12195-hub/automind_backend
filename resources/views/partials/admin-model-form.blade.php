@php $editing = $model !== null; @endphp
<form class="dialog-form" method="POST" action="{{ $editing ? route('admin.catalog.models.update', $model) : route('admin.catalog.models.store') }}">@csrf @if($editing) @method('PATCH') @endif<div class="form-grid"><label><span>Make</span><select name="make_id" required><option value="">Select make</option>@foreach($vehicleMakes as $make)<option value="{{ $make->id }}" @selected($model?->make_id === $make->id)>{{ $make->name_en }}</option>@endforeach</select></label><label><span>Code</span><input name="code" value="{{ $model?->code }}" placeholder="corolla" required></label><label><span>English name</span><input name="name_en" value="{{ $model?->name_en }}" required></label><label><span>Arabic name</span><input name="name_ar" dir="rtl" value="{{ $model?->name_ar }}" required></label><label><span>Start year</span><input name="start_year" type="number" min="1886" max="2200" value="{{ $model?->start_year }}"></label><label><span>End year</span><input name="end_year" type="number" min="1886" max="2200" value="{{ $model?->end_year }}"></label></div><input type="hidden" name="active" value="0"><label class="check-row"><input type="checkbox" name="active" value="1" @checked($model?->active ?? true)><span><strong>Active model</strong><small>Available in the customer vehicle picker.</small></span></label><div class="dialog-actions"><button type="button" class="admin-button secondary" data-close-dialog>Cancel</button><button class="admin-button primary" type="submit">{{ $editing ? 'Save model' : 'Create model' }}</button></div></form>
