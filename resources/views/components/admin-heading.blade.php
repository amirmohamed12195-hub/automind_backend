@props(['title', 'eyebrow', 'description'])
<div class="admin-heading compact-heading">
    <div><span class="admin-eyebrow"><i></i>{{ $eyebrow }}</span><h1>{{ $title }}</h1><p>{{ $description }}</p></div>
</div>
